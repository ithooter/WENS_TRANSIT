"""
sources/ — распознаватели исходников (адаптеры).

Каждый адаптер отвечает на два вопроса: «подходит ли мне этот набор файлов?»
(`matches`) и «прочитать в единый формат» (`read` → canonical.Shipment).
`registry.pick()` пробует адаптеры по порядку — первый подошедший побеждает.

См. АРХИТЕКТУРА_исходников.md §2.
"""
from .registry import pick, read_shipment, ADAPTERS  # noqa: F401
