# Транзитные документы — Веб-приложение. Роадмап

Цель: веб-приложение, в которое любой пользователь заходит, загружает Excel-«Исходник»,
выбирает нужные выходные документы (отдельные листы шаблона, весь шаблон, транзитный
документ, txt HSSC codes, CMR) с указанием Отправителя и Получателя, и скачивает результат.

**Стек:** Flask (Python) · SQLite · серверные HTML-шаблоны (Jinja2) + лёгкий JS.
**Запуск:** локально (`uvicorn`/`flask run`), деплой в облако — позже.
**Форматы Исходника:** `.xlsx`, `.xls`, `.xlsb` (конвертация `.xls/.xlsb` через LibreOffice).

Движки документов взяты из двух Python-проектов:
- **Excel Customs (WENS):** `parties.py`, `company_profiles.py`, `generate_customs_txt.py`,
  `hssc_txt_v3.py`, `fill_optiauto.py`, `fill_nova.py`.
- **Document packing for cars:** `generate_si_documents.py` (+ `classifier.py`).

---

## Статус движков (важно)

| Файл | Статус |
|------|--------|
| `generate_si_documents.py` | перенесён, рабочий |
| `hssc_txt_v3.py`, `generate_customs_txt.py` | перенесены, рабочие |
| `parties.py`, `company_profiles.py` | перенесены, рабочие |
| `fill_optiauto.py` | перенесён (версия из кэша **старее**, чем ждёт `fill_nova`) |
| `fill_nova.py` | перенесён, требует новый `fill_optiauto` (`inv_enrichment`, `pl_dedup_field`) |
| `classifier.py` | **ОТСУТСТВОВАЛ в источниках → создана безопасная заглушка.** Заменить на реальный классификатор ТНВЭД |

Два пробела, которые нужно закрыть от тебя:
1. **`classifier.py`** — твой реальный файл с ключевыми словами и HS-кодами (РЕМНИ 4010310000,
   ПОДШИПНИКИ 8482200009, МАСЛО и т.д.). Сейчас заглушка просто берёт HS-код и описание из строки.
2. **Актуальный `fill_optiauto.py`** — версия с параметрами `inv_enrichment` / `pl_dedup_field`,
   которую использует `fill_nova.py`.

---

## Спринт 0 — Фундамент (этот шаг, СДЕЛАНО)

- [x] Анализ двух проектов и движков документов
- [x] Выбор архитектуры (Flask / локально / все форматы)
- [x] Структура репозитория, `requirements.txt`, `.gitignore`, `README.md`
- [x] Перенос движков в `app/engines/` + заглушка `classifier.py`
- [x] Авторизация: регистрация / логин / выход (SQLite, без верификации)
- [x] Загрузка Исходника (`.xlsx/.xls/.xlsb`) с валидацией
- [x] Экран выбора выходных документов + поля Отправитель/Получатель (подстановка из `parties.py`)
- [x] Сервис-оркестратор: связь выбора с движками
- [x] Страница результата со скачиванием
- [x] `git init` + первый коммит

Результат: приложение запускается локально, проходит сквозной путь
логин → загрузка → выбор → генерация → скачивание.

## Спринт 1 — Стабилизация ядра генерации

- [ ] Подключить реальный `classifier.py` (от тебя) вместо заглушки
- [ ] Прогнать каждый твой реальный Исходник через все 5 типов вывода, зафиксировать ошибки
- [ ] Юнит-тесты на `service.py` для каждого типа вывода (pytest)
- [ ] Корректная обработка ошибок: понятные сообщения вместо стектрейсов
- [ ] Логирование заданий (что сгенерировано, сколько строк, какой профиль)

## Спринт 2 — Шаблоны WENS и режим «заполнить шаблон»

- [ ] Загрузить реальные шаблоны (WENS multi-sheet, OPTIAUTO/NOVA/AUTOEURO) в `app/templates_xlsx/`
- [ ] Включить fill-режим (`fill_optiauto` / `fill_nova`) для «весь шаблон» и «отдельные листы»
- [ ] Обновить `fill_optiauto.py` до версии с `inv_enrichment` / `pl_dedup_field`
- [ ] Выбор пары Отправитель→Получатель из реестра + автоподстановка реквизитов (ИНН/КПП, контракт)
- [ ] Предпросмотр распознанного профиля и числа позиций перед генерацией

## Спринт 3 — Русские переводы инвойсов

- [ ] Подключить переводческий API (когда будет доступ) как модуль `translation/`
- [ ] Кэш переводов (SQLite-таблица `translations`), приоритет: исходник → кэш → API
- [ ] Никогда не «угадывать» перевод: пустое поле, если перевода нет (по твоему правилу)
- [ ] Колонка «Description RU» автоматически в транзитном документе и шаблонах

## Спринт 4 — UX и качество

- [ ] История заданий пользователя (список прошлых генераций, повторное скачивание)
- [ ] Drag-and-drop загрузка, индикатор прогресса
- [ ] Скачивание всех файлов одним ZIP
- [ ] CMR в формате `.doc` (Word) вместо xlsx (как помечено в движке)
- [ ] Валидация форм отправителя/получателя

## Спринт 5 — Деплой (публичный доступ)

- [ ] `Dockerfile` (Python + LibreOffice)
- [ ] Деплой в облако (Render / Railway / Fly.io) с автодеплоем из GitHub
- [ ] Переменные окружения (SECRET_KEY, лимиты), HTTPS
- [ ] Постоянное хранилище для БД и файлов (volume)
- [ ] Базовая защита: лимит размера файла, очистка старых файлов, rate-limit

## Бэклог / на будущее

- Роли (админ/пользователь), сброс пароля по e-mail (когда понадобится верификация)
- Очередь задач (Celery/RQ) для тяжёлых файлов
- Авто-определение пары компаний по содержимому Исходника
- Экспорт TXT с реальной базой HS-единиц (`CD_BLANK_NEW.xlsx`)
